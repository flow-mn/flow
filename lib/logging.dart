import "package:logging/logging.dart";

final Logger mainLogger = Logger("Flow");

final Logger startupLog = Logger("Flow Startup");
final Logger themeLogger = Logger("Theme");

final Logger syncLogger = Logger("Flow Sync");
